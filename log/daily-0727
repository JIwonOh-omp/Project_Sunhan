## 2026-07-27 — GitHub Pages 쇼케이스 2~6단계 (온보딩→랜딩까지 전체 흐름 완성)

### 한 일
- **화면1(온보딩) 구현**: 피부타입(단일선택)·회피 성분(다중선택 6종)·피부 고민(다중선택)
  칩 UI. Continue → `sessionStorage`에 프로필 저장 → 화면2로 연결.
  회피 칩↔한글 성분명 대응표(`AVOID_MATCH_TABLE`)는 4개만 등록(Fragrance/Linalool/
  Limonene/Citral) — Alcohol·Essential oils는 특정 성분명이 아닌 카테고리라 매칭
  대상에서 제외, 매칭 안 돼도 조용히 넘김(추측 금지 원칙 유지)
- **흐름 재배치**: 온보딩 → 제품 검색/선택(Home 신설) → 분석 결과 순서로 변경.
  처음엔 12개 상시 리스트 → 이후 "검색해야 결과가 뜨는" 형태로 다시 좁힘.
  "Scan ingredient label" 버튼 추가(실제 OCR 아님을 오버레이에 명시:
  "Demo — sample scan, not live OCR")
- **제품명 실데이터 반영**: `data/11-cleanser-whipped`(주석 포함 원본)에서 실제 제품명
  확인 → 웹검색으로 영문 정식명 교차검증(YesStyle/Amazon/eBay 등 다수 일치) →
  `product-11.json`의 `name_ko`/`name_en`을 "휩드 머그트리 비건 쿠키 클레이 팩클렌저
  180ml" / "WHIPPED Mugtree Vegan Cookie Clay Pack Cleanser (180mL)"로 확정
- **탭바 4개로 재구성**: Home·Skin Journey·Community·Profile. Scan은 독립 탭에서
  빠지고 Home 내부 진입 요소로 통합. 옛 Scan 탭→다이어리/커뮤니티 Coming soon 배선
  제거
- **Skin Journey 실기능 구현**: 분석 결과 화면에 "Save to Skin Journey" 버튼 →
  `localStorage`에 `{productId, name, savedDate, allergenHits}` 저장(같은 제품
  재저장 시 갱신, 중복 안 쌓임). Skin Journey 탭에서 카드 리스트·재진입·삭제·빈
  상태 처리. "여행 사진 추가"는 정적 Coming soon 카드로 분리(사진 저장 안 함)
- **Community 탭**: 순수 정적 Coming soon 화면으로 분리(서버·게시글 연동 없음)
- **앱 랜딩 화면 신설**: 가치제안 문구 3안 중 사용자가 (b) 채택 —
  "Understand what's really in your Korean skincare." (나머지 2안은 HTML 주석으로
  보존). 로고 + 3점 요약(Translate/Skin alerts/Eco flags) + Get Started + 하단
  정직성 한 줄("No product ads. No medical advice. Ingredient info only."), 슬라이드
  없이 한 화면. 대회/팀 소개는 앱 UI에 넣지 않고 README로 분리 유지
- **진입 흐름 변경**: 기존 온보딩 파일을 `profile.html`로 분리 이동, `index.html`은
  랜딩 전용으로 교체. Get Started 클릭 시 `localStorage` 플래그(`sunhanVisited`)
  기록 → 재방문 시 랜딩 자동 스킵(→ search.html). 로고 탭 또는 `?show=1`로 랜딩
  재열람 가능(발표 시연용 우회 경로)
- **워드마크 스타일링**: 전체 6개 화면의 "sunhan" 워드마크를 Instrument Serif
  italic + 로고와 동일한 짙은 초록(`--accent-dark`)으로 통일. 랜딩에 한글 태그라인
  "순한 · GENTLE" 추가 — 폭이 "sunhan" 글자 폭을 넘지 않도록 자간·크기를 여러 차례
  실측 조정(최종: 11px, letter-spacing 0.6em → 이후 재요청으로 0.3em까지 축소)

### 검증
- 헤드리스 브라우저(Playwright + 로컬 설치 Chromium)로 매 단계 실제 클릭 재현:
  온보딩 선택 → 검색/스캔 → 결과 화면 히트 매칭 → Skin Journey 저장/재진입/삭제 →
  Community/Profile 탭 → 랜딩 재방문 스킵/우회까지 전부 스크린샷·DOM 상태로 확인
- 과정에서 `[hidden]` 속성이 동일 specificity의 클래스 규칙(`display:flex` 등)에
  덮어써지는 CSS 버그를 2회 발견·수정(`analysis.html`, `search.html`) — `[hidden]
  {display:none !important}` 리셋 규칙 추가. 앞으로 `hidden` 토글을 쓰는 새 파일은
  이 리셋을 처음부터 넣기로

### 결과
- 페이지 구조 확정(6개): `index.html`(랜딩) · `profile.html`(온보딩/프로필) ·
  `search.html`(Home, 검색+스캔) · `analysis.html`(결과, Save to Skin Journey 포함) ·
  `skin-journey.html` · `community.html`
- 랜딩→프로필→검색·스캔→결과→Skin Journey/Community 전체 흐름이 실제 클릭 기준
  끊김 없이 연결됨을 확인. 콘솔 에러 없음
- 아직 커밋·push 안 함 — 전부 로컬 작업 디렉토리 상태로 사용자 확인 대기 중

### 다음
- 사용자 확인 후 커밋
- 나머지 11개 제품 실데이터 채우기(현재는 11번 하나만 작동, 나머지는 정직하게
  Coming soon 처리)

## 2026-07-27 (계속) — Pages 배포 안정화 + 쇼케이스 7단계(전 제품 확장) + UX 다듬기

### 한 일
- **Pages 배포 안 반영 이슈 원인 파악·수정**: 실제 페이지 파일은 `docs/`에 있는데
  GitHub Pages 소스가 `main` 루트(`/`)로 설정돼 있어 `docs/index.html` 수정이
  라이브에 반영되지 않던 문제 발견. `data/showcase/product-11.json`을
  `docs/data/showcase/`로 복사하고 `analysis.html`/`search.html`의 fetch 경로를
  `../data/...` → `data/...`로 수정한 뒤 Pages 소스를 `/docs`로 전환(`gh api`)
- **한글 성분명 중앙 정렬**: `.ing-row .names`에 영문/한글 모두 `text-align` 미지정
  상태였음 — 영문이 보통 더 길어 박스 너비를 채우면서 우연히 중앙처럼 보였을 뿐,
  실제로는 둘 다 좌측 정렬. `text-align:center` 추가로 한글도 영문 너비 기준
  중앙에 오도록 수정
- **링크 공유 미리보기(OG/Twitter 메타) + favicon**: 6개 화면 전부에
  `meta description`, `og:*`/`twitter:*` 태그 추가. PowerShell
  `System.Drawing`으로 브랜드 컬러 OG 카드 이미지(1200×630)와 favicon을 직접
  생성(한글 텍스트는 코드포인트로 조립해 스크립트 파일 인코딩 문제 회피)
- **쇼케이스 7단계 — 나머지 11개 제품(01~10, 12) 실데이터 채우기**: Solar API
  재호출 대신, 각 제품 문서의 원문 전성분·시험 포인트 힌트를 근거로 직접
  번역·해설 작성 → 성분명 대응표(`v2_ingredient_map.md`)·EU 알레르겐 참조표
  (`allergen_reference.tsv`, 100종+)·환경 분석 모듈(`environment.md`의 리프세이프+
  미세플라스틱 확정 리스트)에 전부 결정론적으로 대조해 알레르겐/환경 플래그 판정
  (LLM 판단에 맡기지 않음 — 프로젝트가 기존에 지켜온 "구조적 강제 > 모델 자율" 원칙과
  동일). 리프/미세플라스틱은 `environment.md`에 이미 있던 12제품 사전 검증 결과
  (02번 PMMA·MMA크로스폴리머 확정, 09/12번 아크릴레이트 계열 회색지대 등)와 대조해
  일치 확인
  - `status`(피부 자극/알레르겐 배지)와 `environment` 블록(리프/미세플라스틱)은
    서로 다른 축이라는 걸 뒤늦게 인지 — 처음에 자외선차단제 성분에 `status:"caution"`을
    잘못 붙였다가, "For You" 카드가 `environment` 블록만 따로 읽는 구조임을 재확인하고
    `status:"ok"`로 되돌림
  - `search.html`: "Coming soon"/placeholder 배지 제거, 12개 전부 실데이터 검색·선택
    가능하도록 일반화 (제품별 fetch로 이름/카테고리/성분수 로드)
  - `analysis.html`: `?id=` 쿼리 파라미터로 제품 전환하도록 변경(기존엔 11번 하드코딩)
  - `skin-journey.html`: 저장 항목 클릭 시 항상 `analysis.html`(11번)로 가던 것을
    저장된 `productId`로 정확히 돌아가도록 수정
- **caution 성분에 이유 인라인 표시**: 사용자가 "06번 제품 Scented Geranium
  Extract가 왜 caution인지 모르겠다"고 질문 → 각 성분에 이미 있던 `desc_en`이
  화면에 노출되지 않고 있었던 게 원인. 하단 요약 카드 vs 인라인 표시 중 인라인을
  제안(이유가 이미 성분 단위 데이터라 그 성분 바로 옆에 붙이는 게 자연스러움) →
  사용자 승인 후 caution 배지가 붙은 행에 `desc_en`을 배지 아래 한 줄로 렌더링

### 검증
- 신규 12개 JSON 전부: `ingredient_count` vs 실제 배열 길이 일치, `no` 필드
  1..N 연속성, JSON 문법 전부 node로 스크립트 검증
- Playwright(로컬 static 서버 + 기존 설치된 Chromium) 헤드리스 브라우저로 실제
  클릭 흐름 재현: 검색→제품 클릭→`analysis.html?id=`로 이동·실데이터 로딩→
  Save to Skin Journey→Journey 탭에서 같은 항목 클릭 시 같은 제품으로 복귀,
  리프/미세플라스틱 경고 카드 렌더링, caution 인라인 설명 렌더링까지 스크린샷·
  콘솔 에러 확인(전부 없음)
- Pages 빌드는 매 커밋 후 `gh api .../pages/builds/latest`로 상태·커밋 SHA
  일치 확인

### 결과
- 라이브 URL(`https://jiwonoh-omp.github.io/Project_Sunhan/`)이 `docs/`를
  정상 서빙, 12개 제품 전부 검색·분석·Skin Journey 저장까지 끊김 없이 동작
- 제품명(브랜드 영문 표기)은 데이터 신뢰도 대비 상대적으로 약한 지점으로 남음 —
  성분 안전성 판정(알레르겐/환경)은 참조표 대조라 신뢰도가 높지만, 일부 제품의
  "정식 영문 마케팅명"은 제품11만큼 웹검색으로 교차검증하지 않고 직역 수준으로
  채움(사용자에게 고지함)

### 다음
- 제품명 영문 표기 웹검색 교차검증(원하면)
- 라벤더꽃추출물/오렌지오일처럼 "부위·형태 표기가 참조표와 정확히 일치하지 않는"
  알레르겐 판정 케이스들(04·06·09번)에 일관된 처리 기준 정하기
